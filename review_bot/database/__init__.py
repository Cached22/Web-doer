```python
# __init__.py inside review_bot/database directory

# This file is intentionally left empty to define the database package.
# It allows the Python interpreter to recognize the directory as a package
# and enables relative imports between modules within the database package.
```